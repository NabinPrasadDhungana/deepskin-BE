asgiref==3.12.1
Django==6.0.7
django-cors-headers==4.9.0
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
numpy==2.5.1
pillow==12.3.0
PyJWT==2.13.0
sqlparse==0.5.5
drf-spectacular==0.28.0

# ML inference deps -- installed separately, NOT required to run the
# API/workflow itself (mlservice.pipeline lazy-imports these so the
# rest of the backend works and tests pass without a GPU/TF install).
# tensorflow==2.20.0   # see model dev notes for exact required version
# opencv-python        # only needed once real DullRazor + attention-map
#                      # generation is wired into mlservice/pipeline.py
