asgiref==3.12.1
Django==6.0.7
django-cors-headers==4.9.0
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
numpy==2.5.1
pillow==12.3.0
PyJWT==2.13.0
sqlparse==0.5.5
drf-spectacular==0.28.0
python-dotenv==1.2.2
celery==5.4.0
redis==5.0.8

# Production static + cloud media storage (Render has an ephemeral disk,
# so uploaded lesion images/attention maps live in Backblaze B2 via S3).
whitenoise
django-storages[boto3]

# Production webserver + Postgres (Render)
gunicorn>=21.2.0
psycopg2-binary
dj-database-url

# HTTP client for the hosted HF inference Space (async callback webhook)
requests

# ML inference deps are intentionally NOT in this file: the TensorFlow model
# runs on Hugging Face Spaces, not in the Django container. If you need to
# run inference locally, install tensorflow==2.20.0, opencv-python and
# pillow in the venv (see mlservice/pipeline.py).
